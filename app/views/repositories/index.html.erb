<div class = "container">

  <h1>Repos</h1>
  <!-- List repositories and display username here! -->
  
    <p>
      Currently logged in as: <%= @info["login"] %>
    </p>


  <%= form_tag("/repositories/create", method: "post") do %>
    <%= label_tag(:name, "Create a New Repo:") %>
    <%= text_field_tag(:name, nil, id: "new-repo") %>
    <%= submit_tag("Create") %>
  <% end %>


  <p>
    <% @repos.map do |repo| %>
      <%= repo["name"] %><br>
    <% end %>
  </p>


</div>
